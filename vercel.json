{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/scan/:path*",
      "destination": "/api/scan/:path*"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
