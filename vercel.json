{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/[...path].ts"
    },
    {
      "source": "/scan/:path*",
      "destination": "/api/[...path].ts"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
